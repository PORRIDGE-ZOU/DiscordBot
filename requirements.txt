# Discord library. py-cord (Pycord) — chosen for built-in voice recording Sinks.
# NOTE: py-cord and discord.py both install a module literally named `discord`.
# Never install both into the same environment; they collide.
py-cord>=2.6,<3

# Loads secrets from a .env file into environment variables at startup.
python-dotenv>=1.0

# Official Notion SDK. Synchronous client — call it off the event loop in async code.
notion-client>=2.2

# Scheduler for one-shot + weekly reminders. Pinned to the 3.x API (4.x is a
# different API). SQLAlchemy backs the persistent SQLite jobstore so reminders
# survive bot restarts.
apscheduler>=3.10,<4
SQLAlchemy>=2.0

# OpenAI SDK — parses the natural-language #time-off posts into dated entries.
# Synchronous client; call it off the event loop with asyncio.to_thread.
openai>=1.40
